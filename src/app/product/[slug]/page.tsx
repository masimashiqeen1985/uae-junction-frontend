import type{Metadata}from'next'
import Link from'next/link'
interface Props{params:Promise<{slug:string}>}
export async function generateMetadata({params}:Props):Promise<Metadata>{const{slug}=await params;return{title:slug.replace(/-/g,' ')}}
export default async function ProductPage({params}:Props){const{slug}=await params;return(<div className="container-xl py-12 max-w-5xl"><nav className="text-sm text-neutral-400 mb-6"><Link href="/" className="hover:text-primary">Home</Link> / <span>{slug.replace(/-/g,' ')}</span></nav><div className="grid grid-cols-1 lg:grid-cols-2 gap-10"><div className="bg-neutral-100 rounded-card aspect-[4/3] flex items-center justify-center text-neutral-400">Product image</div><div><h1 className="font-display font-bold text-2xl text-neutral-900 mb-4">{slug.replace(/-/g,' ')}</h1><p className="text-neutral-500 mb-6">Full product details loading from CMS...</p><button className="w-full bg-primary hover:bg-primary-dark text-white font-bold py-4 rounded-btn text-lg transition-colors">Book Now</button></div></div></div>)}